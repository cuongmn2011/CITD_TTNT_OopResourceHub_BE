# Environment Configuration
# Môi trường chạy: development, production, staging
ENVIRONMENT=development

# Database Configuration - REQUIRED (PostgreSQL only)
# Sử dụng Neon PostgreSQL cho cả development và production
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# Ví dụ với Neon Database (Khuyến nghị):
# DATABASE_URL=postgresql://neondb_owner:password@ep-xxx-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require

# Hoặc PostgreSQL local:
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Hoặc Google Cloud SQL:
# DATABASE_URL=postgresql://user:password@/dbname?host=/cloudsql/project:region:instance
