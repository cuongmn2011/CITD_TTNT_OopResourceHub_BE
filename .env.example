# Environment Configuration
# Môi trường chạy: development, production, staging
ENVIRONMENT=development

# Database Configuration
# Để sử dụng PostgreSQL, uncomment và điền thông tin kết nối
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Để sử dụng SQLite (mặc định cho local development), để trống hoặc:
# DATABASE_URL=sqlite:///./oop_resource.db

# Vercel Postgres (sẽ tự động inject khi deploy lên Vercel)
# DATABASE_URL=postgres://default:xxxxx@xxxxx.postgres.vercel-storage.com:5432/verceldb
